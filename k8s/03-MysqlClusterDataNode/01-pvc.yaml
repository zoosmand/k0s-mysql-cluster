---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-cluster-data-node-2-cache
  namespace: mysql-cluster
  labels:
    maxSize: 20Gi
spec:
  storageClassName: mysql-data-mp1
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi

...

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-cluster-data-node-3-cache
  namespace: mysql-cluster
  labels:
    maxSize: 20Gi
spec:
  storageClassName: mysql-data-mp8
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi

...

